# Core dependencies for MetaPython
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scipy>=1.10.0,<2.0.0
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0

# R integration
rpy2>=3.5.0,<4.0.0

# API framework (if using FastAPI based on CI/CD jobs)
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
pydantic>=2.0.0,<3.0.0

# Optional: ML dependencies (heavy - install only when needed)
# torch>=2.0.0
# transformers>=4.30.0
# scikit-learn>=1.3.0
