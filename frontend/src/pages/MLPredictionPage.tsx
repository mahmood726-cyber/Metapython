import React from 'react';
import { Box, Typography } from '@mui/material';

const MLPredictionPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">MLPredictionPage</Typography>
    </Box>
  );
};

export default MLPredictionPage;
