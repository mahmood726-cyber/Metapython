import React from 'react';
import { Box, Typography } from '@mui/material';

const ReportingPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">ReportingPage</Typography>
    </Box>
  );
};

export default ReportingPage;
