import React from 'react';
import { Box, Typography } from '@mui/material';

const CollaborationPage: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">CollaborationPage</Typography>
    </Box>
  );
};

export default CollaborationPage;
